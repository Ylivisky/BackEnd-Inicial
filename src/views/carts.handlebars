<h1>Listado de Carritos</h1>

{{#each carts}}
<div style="border: 1px solid black; margin-bottom: 10px; padding: 10px;">
    <h3>🛒 Carrito ID: {{_id}}</h3>
    <ul>
        {{#each products}}
        <li>
            <strong>Producto:</strong> {{product.title}}<br>
            <strong>Cantidad:</strong> {{quantity}}
        </li>
        {{/each}}
    </ul>
    <a href="/carts/{{_id}}">Ver carrito completo</a>
</div>
{{/each}}